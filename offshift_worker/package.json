{
  "name": "offshift-worker-demo",
  "version": "0.1.0",
  "private": true,
  "description": "Standalone Offshift Cloudflare Worker demo API.",
  "type": "module",
  "scripts": {
    "build:widget": "node ./scripts/sync-widget.mjs",
    "dev": "npm run build:widget && wrangler dev",
    "deploy": "npm run build:widget && wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "rm -rf .test-dist && tsc --noEmit false --outDir .test-dist && node --test test/*.test.mjs"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "wrangler": "^4.24.3"
  }
}
